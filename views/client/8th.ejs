<%- include('../includes/header.ejs') %>

</head>

<body>
    <%- include('../includes/navbar.ejs') %>
<main>
    <h1>6th class</h1>
    <div class="container">
        <div class="row">
            <% if (students.length > 0) { %>
                <% for (let student of students) { %>
            <div class="col-lg-6">
                <h1> <%= student.name %></h1>
                <h2><%= student.phone %></h2>
                <h2><%= student.medium %></h2>
                <h2><%= student.adnum %></h2>
                <a href="/class8/<%= student.id %>">send sms</a>
            </div>
            <% } %>
      
            <% } %>
        </div>
    </div>
</main>

<%- include('../includes/footer.ejs') %>